<% if user_signed_in? %>
  <li><%= link_to t('users.edit_registration'), edit_user_registration_path) %></li>
<% else %>
  <li><%= link_to t('users.sign_up'), new_user_registration_path  %></li>
<% end %>
