<p><%= t('devise.confirmations.greetings') %></p>

<p><%= t('devise.confirmations.confirmation_mail_text') %></p>

<p><%= link_to t('devise.mailer.confirm_account'), confirmation_url(@resource, :confirmation_token => @resource.confirmation_token) %></p>
