        <tr>
            <th colspan="2" class="title"><%= t('profile.subscriptions') %></th>
        </tr>
        <tr>
            <td><%= t 'activerecord.attributes.user.email' %></td>
            <td>
                <div class="field"><%= f.email_field :email %></div>
            </td>
        </tr>
        <%= f.fields_for :profile do |profile_form|%>
          <%= render 'profiles/news_comments_subscription', :f => profile_form %>
        <% end %>

